---
import { SITE_TITLE } from '../consts';
import HeaderLink from './HeaderLink.astro';
---

<header>
	<nav>
		<h2>
			<a href="/">
				<img src="/logo.png" alt="Tech Oculta" class="site-logo" />
				<!-- Caso a imagem falhe, o alt text vai aparecer -->
			</a>
		</h2>
		<div class="internal-links">
			<HeaderLink href="/">Início</HeaderLink>
			<HeaderLink href="/blog">Ofertas</HeaderLink>
			<HeaderLink href="/about">Sobre</HeaderLink>
		</div>
	</nav>
</header>
<style>
	header {
		margin: 0;
		padding: 0 1em;
		background: #000000; /* Fundo totalmente preto para esconder a borda preta da logo */
		border-bottom: 1px solid rgba(0, 255, 163, 0.15);
		position: sticky;
		top: 0;
		z-index: 100;
	}
	h2 {
		margin: 0;
		display: flex;
		align-items: center;
	}
	h2 a {
		display: flex;
		justify-content: center;
		align-items: center;
		width: 160px; /* Largura visível da logo */
		height: 60px; /* Altura visível da logo */
		overflow: hidden; /* Corta tudo o que passar (a borda preta) */
		text-decoration: none;
		color: var(--text-main);
	}
	.site-logo {
		min-width: 300px; /* Tamanho real enorme para a imagem */
		min-height: 300px;
		object-fit: contain;
		transition: transform 0.2s ease;
	}
	.site-logo:hover {
		transform: scale(1.05);
	}
	nav {
		display: flex;
		align-items: center;
		justify-content: space-between;
		max-width: 960px;
		margin: 0 auto;
	}
	nav a {
		padding: 1.2em 0.8em;
		color: var(--text-muted);
		border-bottom: 4px solid transparent;
		text-decoration: none;
		font-weight: 600;
		transition: all 0.3s ease;
	}
	nav a:hover, nav a.active {
		color: var(--accent);
		border-bottom-color: var(--accent);
		text-shadow: 0 0 8px rgba(0, 255, 163, 0.3);
	}
	@media (max-width: 720px) {
		h2 a {
			width: 120px;
			height: 50px;
		}
		.site-logo {
			min-width: 220px;
			min-height: 220px;
		}
		nav a {
			padding: 0.8em 0.5em;
			font-size: 0.9em;
		}
	}
</style>
