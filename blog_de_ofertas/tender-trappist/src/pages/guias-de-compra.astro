---
import { Image } from 'astro:assets';
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import Icon from '../components/Icon.astro';
import { SITE_TITLE } from '../consts';

// Pega todos os posts do blog
const allPosts = await getCollection('blog');

// Filtra estritamente apenas os guias de compra
const guidePosts = allPosts
	.filter(post => {
		if (post.data.articleType === 'guide') return true;
		return post.data.title.toLowerCase().includes('melhores') || post.data.title.toLowerCase().includes('guia');
	})
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<!doctype html>
<html lang="pt-BR">
	<head>
		<BaseHead 
			title={`Guias de Compra - ${SITE_TITLE}`} 
			description="Descubra quais são as melhores opções do mercado para o seu orçamento em diversas categorias." 
		/>
		<style>
			main {
				width: 100%;
				max-width: 1080px;
				margin: 0 auto;
				padding: 0 1rem;
				box-sizing: border-box;
			}
			.page-title {
				text-align: center;
				margin-bottom: 0.3em;
				color: var(--text-main);
			}
			.page-subtitle {
				text-align: center;
				color: var(--text-muted);
				margin-bottom: 2em;
				font-size: 1.1em;
			}
			.empty-state {
				text-align: center;
				padding: 4rem 1rem;
				background: var(--bg-card);
				border-radius: var(--radius-l);
				border: 1px solid var(--line-subtle);
				margin-bottom: 3rem;
			}
			.empty-state h3 {
				color: var(--text-main);
				margin-bottom: 0.5rem;
			}
			.empty-state p {
				color: var(--text-muted);
				max-width: 500px;
				margin: 0 auto;
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<h1 class="page-title">Guias de Compra</h1>
			<p class="page-subtitle">Não sabe o que escolher? Nossos guias te ajudam a encontrar o melhor produto para o seu perfil.</p>
			
			<section>
				{
					guidePosts.length > 0 ? (
						<ul class="articles-grid">
							{
								guidePosts.map((post, index) => (
									<li class={`article-card${index === 0 ? ' article-card--lead' : ''}`}>
										<a href={`/blog/${post.id}/`}>
											<div class="article-card__media">
												{post.data.heroImage && (
													<Image width={720} height={360} src={post.data.heroImage} alt={post.data.title} />
												)}
											</div>
											<div class="article-card__body">
												{post.data.category && (
													<span class="tag">{post.data.category}</span>
												)}
												<h3 class="article-card__title">{post.data.title}</h3>
												<span class="card-cta">Ler o Guia <Icon name="arrowRight" size={14} /></span>
											</div>
										</a>
									</li>
								))
							}
						</ul>
					) : (
						<div class="empty-state">
							<h3>Nenhum guia encontrado</h3>
							<p>Ainda estamos preparando novos guias para você.</p>
						</div>
					)
				}
			</section>
		</main>
		<Footer />
	</body>
</html>